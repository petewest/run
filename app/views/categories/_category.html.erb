<li>
	Name: <%= link_to category.name, category %>
	<% if is_admin? %>
		| <%= link_to "Edit", edit_category_path(category) %>
		| <%= link_to "Delete", category, method: :delete, data: {confirm: "This will delete the category and all posts, are you sure?"} %>
	<% end %>
</li>