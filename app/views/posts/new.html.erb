<% provide(:title,'New post')%>


<div class="row">
	<div class="col-md-12">
		<%= form_for(@post) do |f| %>

			<%= render 'fields', f: f %>

			<%= f.submit "Create post", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>
